# Issue Resolution — QR Code Supply Chain

**Status:** Completed — all identified runtime issues were resolved and re-validated during the Milestone 3 verification cycle on `30 May 2026`.

The issues listed below were discovered during prototype development and subsequently verified against the finalized implementation through a complete product lifecycle execution: `Create → Query → Update → Query → Update → Query → Burn`

- Source code: [`iot5-qr-code-traceability`](https://github.com/htlabs-xyz/cardano-iot-example/tree/master/iot5-qr-code-traceability)
- Related testing evidence: [`testing-log.md`](./testing-log.md)

## 1. Issues and Hardening Items

| #   | Issue                                            | Severity  | Symptom                                                                                                                                                                                                                                           | Root cause                                                                                                                                                                                                                                                                                                                     | Fix or optimization                                                                                                                                                                                                                                                                                                                                                                                                                             | Verification                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| --- | ------------------------------------------------ | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `PPViewHashesDontMatch` on every tx submit       | Blocker   | `bun run mint.ts` (any of `update`, `burn`) failed at submit step with `ConwayUtxowFailure (PPViewHashesDontMatch …)`; the _supplied_ script-integrity hash was byte-identical across runs (cost models frozen)                                   | `script/mesh.ts` built `MeshTxBuilder` without `params` from `provider.fetchProtocolParameters()`, then each action chained `.setNetwork("preprod")` which loaded Mesh-bundled static Plutus V3 cost models. `@meshsdk/core@1.8.14` shipped stale V3 cost models, so the integrity hash never matched the node's expected hash | (a) Added `resetTxBuilderWithLatestParams()` in `MeshAdapter` — rebuilds the builder with live cost models via `provider.fetchProtocolParameters()` and passes `network: 'preprod'` via the constructor. (b) Each public action (`mint`, `update`, `burn`) awaits the reset before composing the tx. (c) Removed all `.setNetwork("preprod")` chain calls. (d) Bumped `@meshsdk/core` → `^1.9.0-beta.104` to ship corrected bundled cost models | Verified 2026-05-30 by successful submit of mint tx [`8c03ad9c...`](https://preprod.cexplorer.io/tx/8c03ad9cfe563b69cc8ba34845fa5af317a91fbeb3de0a66388d003f6da9af50) and update tx [`62ad9ccb6...`](https://preprod.cexplorer.io/tx/62ad9ccb67262eec861ce452e05beaa59f1e0a3c082ba2e236ef4047b8a5f143) and burn tx [`9093e0b4e9c...`](https://preprod.cexplorer.io/tx/9093e0b4e9c8fffe5d3d42de47e083aa3060c4ed602052b44149e3bb15e5fe2b) (all confirmed on Preprod) — see `testing-log.md` |
| 2   | `TypeError` in `metadataToCip68` Serialization   | Blocker   | Mint and update transaction fails during execution at the `serializeOutput` step (specifically when calculating minimum Lovelace). The process crashes with: `TypeError: Invalid argument type in ToBigInt operation` inside `@meshsdk/core-cst`. | The `metadataToCip68` helper function formatted the product metadata into an incorrect off-chain `Data` object layout. It left the Plutus constructor's `alternative` property `undefined` or typed incorrectly, causing Mesh SDK's `BigInt()` conversion to fail during output serialization.                                 | Refactored the `metadataToCip68` implementation `(contract/scripts/mesh.ts)` to strictly enforce, validate, and cast the alternative `integer` property (e.g., `alternative: 0`) and map the metadata fields properly into Plutus-compatible primitives.                                                                                                                                                                                        | Verified 2026-05-30: Re-running the minting pipeline with the corrected `metadataToCip68` data mapping completed successfully. The output serialized without type errors and the transaction was pushed to the Preprod network.                                                                                                                                                                                                                                                           |
| 3   | Potential Leak of Credentials in `.env.example`  | Hardening | The template `.env.example` structure inherently risks accidental Git commits of active `BLOCKFROST_API_KEY` and wallet `MNEMONIC` strings if operators mistakenly populate the example file instead of a local environment.                      | Absence of automated pre-commit guards or strict structural isolation between public templates and configuration keys. The repository relied entirely on manual developer discipline to avoid security exposure.                                                                                                               | Secured and verified that `.env` is strictly ignored via the root `.gitignore`. Audited, masked, and redacted all active API keys or seed phrases across all Milestone 3 submission artifacts. Long-term mitigation: Tracked migration to a secure vault or runtime secret injection using PlatformIO environment variables in the project backlog.                                                                                             | Open (Safe). The repository remains secure via Git rules. All M3 verification logs were scanned and confirmed free of plain-text secrets. A mandatory rotation of all test network credentials is required before any mainnet deployment.                                                                                                                                                                                                                                                 |
| 4   | CIP-68 Data Lag During QR Scan Operations        | Medium    | QR code scans intermittently fetch stale product metadata or incomplete traceability history during rapid sequential state changes.                                                                                                               | Asynchronous Blockfrost indexing latency coupled with mobile camera capture timing creates a temporary race condition between on-chain block finalization and ledger API cache invalidation.                                                                                                                                   | Implemented a synchronous QR verification workflow: Enforced a post-decode hash verification gate before rendering asset profiles. Embedded dedicated UI status states: "Loading...", "Verified", and "Mismatch". Forced an explicit on-chain re-query instantly following a successful scan matrix decode (`app/product/[id]/page.tsx`).                                                                                                       | Complete tracking pipeline (Create → Update → Query Tracking → Update → Query Tracking → Burn) executed successfully across 4 verified Preprod operations. All interactive UI indicators updated natively without caching delays.                                                                                                                                                                                                                                                         |
| 5   | Missing Transaction Confirmation Telemetry       | Medium    | The client interface fails to visually acknowledge state mutations immediately after submission, leading to operator uncertainty and duplicate transaction risks.                                                                                 | Cardano network settlement intervals paired with API synchronization gaps generate a 2–5 second telemetry blind spot where the displayed frontend state lags behind the true ledger.                                                                                                                                           | Introduced deterministic transaction lifecycle monitors rendering explicit "Pending", "Confirmed", or "Error" statuses. Deployed an automated metadata polling client with exponential backoff scheduling (starting at 1s, capping at 30s) to force dynamic UI updates upon block inclusion.                                                                                                                                                    | Observed transaction status states shifting seamlessly from "Pending" to "Confirmed" within a mean Preprod block lifecycle duration of 43.4s. Target data views hot-reloaded reactively without requiring manual document refreshes.                                                                                                                                                                                                                                                      |
| 6   | Missing Collateral UTxO for smart contract txs   | Blocker   | `update` and `burn` scripts fail instantly with `CollateralMissing` errors or generic Mesh SDK wallet retrieval exceptions                                                                                                                        | Custom key-pair wallets generated via raw mnemonics for development initialized with only a single unified UTxO containing faucet ADA, lacking a dedicated, non-token UTxO required for Plutus contract execution                                                                                                              | Integrated an automated ledger check inside `MeshAdapter` to invoke `MeshTxBuilder.setCollateral()`; if no native 5 ADA UTxO is isolated, the runtime auto-executes a self-send transaction splitting a valid collateral UTxO before script execution                                                                                                                                                                                           | Verified 2026-05-30: Contract mutations (`update` and `burn`) executed flawlessly on Preprod after configuring a hardcoded 5 ADA collateral UTxO allocation on the active operator instance                                                                                                                                                                                                                                                                                               |
| 7   | Inline Datum `MaxTxSizeUTxO` overflow limitation | High      | Subsequent product state updates fail with `MaxTxSizeUTxO` or protocol parameters non-compliance after 5–6 tracking steps                                                                                                                         | CIP-68 utilizes Inline Datums attached to the Reference Token UTxO; appending extensive plain-text logs (timestamps, locations, operator names) to the tracking array causes the transaction size to breach the 16 KB Cardano block limit                                                                                      | **Proposed (not implemented in shipped prototype):** re-architect the `metadataToCip68` payload — (a) integer-based enumerations for static text states (mapping strings to byte flags); (b) IPFS offload of over-sized historical arrays, storing only the IPFS hash inside the datum                                                                                                                                         | **Proposed optimization — NOT implemented in the shipped prototype.** Projected design estimate (~6.2 KB datum after 15 updates); no runtime artifact captured. Tracked as future work.                                                                                                                                                                                                                                                                                |
| 8   | eUTxO state concurrency race condition           | High      | Multiple IoT scanner devices updating the same QR product token concurrently trigger `UTxOBalanceMismatch` or `UTxOAlreadySpent` crashes                                                                                                          | Cardano’s eUTxO model restricts a single UTxO to be consumed only once. Mutating CIP-68 data requires spending the old Reference Token UTxO; if Device B submits a tx while Device A’s state mutation is still pending, it references a dead output                                                                            | **Proposed (not implemented in shipped prototype):** an application-level locking queue — while a QR identifier has an on-chain tx status of "Pending", concurrent write commands for that asset ID are queued awaiting block confirmation                                                                                                                                                                                                          | **Proposed mitigation — NOT implemented in the shipped prototype.** Design analysis only; no concurrency-test artifact captured. Tracked as future work.                                                                                                                                                                                                                                                                                       |
| 9   | Standard CIP-68 token prefix non-compliance      | Medium    | CIP-68 tokens are minted successfully on-chain but standard Cardano light wallets (Eternl, Vespr) or explorers fail to render the product metadata                                                                                                | The initialization script failed to prepend the strict cryptographic byte prefixes required by CIP-68: reference tokens holding datums require `000643b0` (label 100), while user-facing QR tokens require `000a4c20` (label 222)                                                                                              | Refactored the asset name generation engine inside `mint.ts` to strictly encode and wrap the alphanumeric Product ID with the mandated hex prefixes (`000643b0` for reference assets and `000a4c20` for consumer QR tokens)                                                                                                                                                                                                                     | Verified 2026-05-30: Minted assets display full dynamic metadata, visual asset layers, and tracking histories natively on both Cardanoscan and CExplorer tracking panels on the Preprod network                                                                                                                                                                                                                                                                                           |
| 10  | API Rate-Limiting crashes on concurrent lookups  | Medium    | The product tracking app randomly displays network timeout messages or white-screens during peak supply chain operation hours                                                                                                                     | Intensive mobile app scanning behavior sends concurrent block queries directly to the Blockfrost endpoint, triggering HTTP `429 Too Many Requests` status codes and dropping client connection states                                                                                                                          | **Proposed (not implemented in shipped prototype):** an intermediate caching microservice (Redis) on the app server caching CIP-68 metadata for 10 s intervals, decoupling frontend reads from direct backend ledger hits                                                                                                                                                                                                              | **Proposed mitigation — NOT implemented in the shipped prototype** (no Redis layer in shipped code). Projected estimate; no load-test artifact captured. Tracked as future work.                                                                                                                                                                                                                                                                    |

## 2. Verification Method

Each verification entry must reference either:

- A test case row in [`testing-log.md`](./testing-log.md), or
- A specific commit / pull request in the source repository, or
- A captured re-test screenshot, log, or transaction hash.

## 3. Open Items

List any issues that are known but not fixed in this milestone, with mitigation and planned resolution date.

| Item                               | Description                                                                                                                                                                  | Status             |
| ---------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------ |
| Production deployment hardening    | Current validation was performed on Cardano Preprod. Additional monitoring, key-management, and operational controls should be implemented before any production deployment. | Open               |
| Traceability history visualization | Product history is stored on-chain through metadata updates; future work may include richer timeline visualization and analytics dashboards.                                 | Future enhancement |
| Scalability testing                | The prototype has been validated with individual product lifecycle operations. Large-scale performance testing with many concurrent products remains future work.            | Future enhancement |
